---
cover: /images/projects/covers/mandelbrot_cover.webp
title: Mandelbrot
description: Visualização do conjunto de Mandelbrot e de Julia em WebGL.
tags: [JavaScript, p5.js, WebGL]
date: 2016-08-03
---

Visualização do conjunto de [Mandelbrot](https://en.wikipedia.org/wiki/Mandelbrot_set) e do conjunto de [Julia](https://en.wikipedia.org/wiki/Julia_set) gerados por shader WebGL.

import { Mandelbrot } from '@projects/mandelbrot';

<Mandelbrot client:only="react" />

$f(z) = z^2 + c$

```glsl
int iterate(vec2 p, vec2 c) {
	const int maxIterations = 100;
	const float threshold = 16.0;

	int result = 0;

	for (int i = 0; i < maxIterations; i++) {
		result++;

		float _x = p.x * p.x - p.y * p.y + c.x;
		float _y = 2.0 * p.x * p.y       + c.y;

		float mag_sqr = _x * _x + _y * _y;
		if (mag_sqr > threshold) {
			break;
		}

		p.x = _x;
		p.y = _y;
	}

	return result;
}
```

## Referências

- [Coding Train](https://www.youtube.com/watch?v=6z7GQewK-Ks)
